"""
Задание №1
� Вспомните какие модули вы уже проходили на курсе.
� Создайте файл, в котором вы импортируете встроенные в модуль функции под псевдонимами. (3-7 строк импорта).
"""

import random as rnd
from calendar import formatstring as frm_str
from decimal import getcontext as gc
import sys._monitoring as sm
from math import sqrt as sq, remainder as rm, factorial as fact
